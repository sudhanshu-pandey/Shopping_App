import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        marginHorizontal: 30
    },
    flatlistcontainer: {
        marginTop: 30
    }
});

export default styles;